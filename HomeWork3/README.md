# AI Image Chat — Домашняя работа 3

Приложение на C# / WinForms, которое принимает команды на **естественном языке** (русском или английском), интерпретирует их через локальную языковую модель (**LM Studio**) и применяет соответствующую обработку к изображению с помощью **OpenCV**.

---

## Как работает нейросеть

```
Пользователь пишет текст
        ↓
LM Studio (локальная LLM у студента) получает запрос через OpenAI-совместимый API
        ↓
Модель возвращает JSON с командой, например:
    {"type":"rotate","angle":90,"reply":"Поворачиваю на 90°."}
        ↓
Приложение разбирает JSON и вызывает нужную функцию OpenCV
        ↓
Результат отображается в окне
```

**Нейросеть не обрабатывает изображение сама** — она только переводит естественный язык в структурированную команду. Всю обработку выполняет OpenCV.

---

## Стек

| Компонент | Версия | Роль |
|-----------|--------|------|
| .NET | 8.0-windows | платформа |
| WinForms | встроен | интерфейс |
| [OpenCvSharp4](https://github.com/shimat/opencvsharp) | 4.9.0 | обработка изображений |
| [LM Studio](https://lmstudio.ai/) | любая | локальный LLM-сервер |
| [ngrok](https://ngrok.com/) | любая | туннель для удалённого доступа |

---

## 5 основных команд

| Кнопка / фраза | Действие | Функция OpenCV |
|----------------|----------|----------------|
| **↔ Перевернуть** / «отразить зеркально» | Зеркальное отражение по горизонтали | `Cv2.Flip` |
| **📐 Размер 800px** / «уменьшить до 800 пикселей» | Изменение разрешения с сохранением пропорций | `Cv2.Resize` |
| **👤 Найти лица** / «найти и выделить лица» | Детекция лиц каскадом Хаара | `CascadeClassifier.DetectMultiScale` |
| **⬛ Чёрно-белое** / «сделать чёрно-белым» | Перевод в оттенки серого | `Cv2.CvtColor` |
| **✏ Края** / «обнаружить края» | Выделение контуров алгоритмом Кэнни | `Cv2.Canny` |

---

## Структура проекта

```
HomeWork3/
├── Models/
│   └── ImageCommand.cs       # модель команды (enum + параметры)
├── Services/
│   ├── ImageProcessor.cs     # реализация всех операций OpenCV
│   └── LlmService.cs         # запрос к LM Studio, разбор JSON
├── MainForm.cs               # WinForms UI (чат + изображение)
├── SettingsForm.cs           # диалог смены URL сервера
├── Program.cs                # точка входа
└── ImageAI.csproj
```

---


## Дополнительные команды (через текстовый ввод)

Поворот, стили (аниме, Disney, эскиз, масляная живопись, акварель), извлечение цветового канала, размытие, регулировка яркости/контраста, удаление области с inpainting.

---

## Использованные инструменты

- C# / .NET 8 / WinForms
- [OpenCvSharp4](https://github.com/shimat/opencvsharp)
- [LM Studio](https://lmstudio.ai/)
- Claude Sonnet 4.6
